# ── Secrets & credentials — never commit real values ──
secret.yaml
secrets/
*.secret.yaml
*-secret.yaml
.env
.env.*
!.env.example
*.pem
*.key
*.crt
*.p12
kubeconfig
kubeconfig.*
*.kubeconfig

# ── Kustomize local overrides (if you generate per-machine overlays) ──
kustomization.local.yaml

# ── Terraform (in case infra-as-code gets added later) ──
*.tfstate
*.tfstate.*
.terraform/
.terraform.lock.hcl

# ── Docker ──
docker-compose.override.yaml
.docker/

# ── Python (FastAPI/AI service) ──
__pycache__/
*.py[cod]
*.egg-info/
*.egg
.eggs/
venv/
.venv/
env/
.mypy_cache/
.pytest_cache/
.ruff_cache/
*.log

# ── Node / React (frontend or tooling) ──
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
dist/
build/
.next/
.turbo/
.cache/

# ── Coverage / test artifacts ──
coverage/
.coverage
htmlcov/
*.cover

# ── OS files ──
.DS_Store
Thumbs.db
desktop.ini

# ── IDE / editor ──
.vscode/
.idea/
*.swp
*.swo
*~

# ── Misc local scratch ──
*.local
.tmp/
tmp/