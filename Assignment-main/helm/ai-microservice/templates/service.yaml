apiVersion: v1
kind: Service
metadata:
  name: {{ include "ai-microservice.fullname" . }}-svc
spec:
  type: {{ .Values.service.type }}
  selector: {app: ai-microservice}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
